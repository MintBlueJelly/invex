# Known bugs

Every row here is pinned by at least one test using the `knownBug()` helper
(`test-utils/knownBug.ts`), and every id used by a test appears here. Both directions are
enforced by `packages/core/test/unit/knownBugs.registry.test.ts`, so this file cannot drift
away from the suite: removing the last test for an id forces removing its row, and adding a
row forces adding a test.

**Status.** `open` — the defect exists and its pins are `it.fails()`. `fixed` — repaired; the
pins were promoted to ordinary tests and the row is kept for history. A row that is `fixed`
must have no `knownBug()` call left referencing it.

Run `pnpm test:known-bugs` to see what is still broken. That run is *expected* to be red.

**Severity** is deployment-weighted, not code-weighted, per `DEPLOYMENT.md`: single replica,
`Recreate`, in-process worker, every ingested PDF stored only in Postgres.

- **critical** — silently produces wrong financial data, or wedges the queue.
- **high** — loses data, blocks a document class entirely, or discloses internals.
- **medium** — wrong behaviour with a visible symptom.

| id | severity | status | defect | where | pinned by |
| --- | --- | --- | --- | --- | --- |
| INVEX-004 | critical | fixed | `PUT /api/review/:id` validated shape only (`zCanonicalInvoice` is a plain `z.object`, no refinements), never re-ran the solver, and then induced a **vendor template** from the unchecked numbers — so one typo in review anchored wrong values for every future invoice from that vendor. | `packages/server/src/http/review.ts:60` | `packages/server/test/component/http/review.arithmetic.test.ts` |
| INVEX-002 | critical | fixed | OCR column bands were assigned by a token's left edge with a flat 0.02 tolerance. Amount columns are right-aligned, so a value wider than its header starts left of it and fell into the previous column — shifting the **whole row** one column across: `quantity` became `"2199.50"` (quantity fused with unit price) and `unitPrice` silently held the line total. Every value stayed schema-valid. | `packages/core/src/template/applyOcr.ts:77` | `packages/core/test/unit/template/applyOcr.test.ts` |
| INVEX-001 | critical | fixed | `amountVariants()` included the raw dot-decimal form in **both** locale candidate sets, so `detectLocale` could never return `"."`. An ungrouped English page was induced as `decimal: ","`, and the persisted template then read every amount 100× too large — permanently, for that vendor. Silent whenever the VAT arithmetic is exact (round net amounts), because scaling preserves every constraint; caught by `C4_VAT_SUM` only when a rounding residue exists to be scaled with it. | `packages/core/src/template/induce.ts:57` | `packages/core/test/unit/template/induce.locale.test.ts` |
| INVEX-005 | high | fixed | `normBbox` defaulted a missing `pages[].size` to `1`, so every bbox in the document normalized to `[1,1,1,1]`. All positional logic — region anchors, the classifier's heading-position feature, the segmenter, OCR x-bands — then ran on degenerate geometry with no diagnostic. | `packages/core/src/docling/mapDocument.ts:67` | `packages/core/test/unit/docling/mapDocument.test.ts` |
| INVEX-006 | critical | fixed | `rasterizePdf` had no page-dimension guard. A PDF declaring a 200x200 inch MediaBox asks for ~30000x30000 = 900 MP (~3.6 GB RGBA) at the configured 150 dpi; the OOM kill rolls the claim transaction back **without incrementing `attempts`**, so the document is re-claimed first on every restart and wedges the queue. | `packages/server/src/pdf/rasterize.ts:17` | `packages/server/test/unit/pdf/rasterize.test.ts` |
| INVEX-011 | high | fixed | Three defects in the vendor template store: `version` computed in application code (lost update); `listTemplates` sorted **ascending** by `updatedAt`, returning the oldest templates and never newly learned ones; and a non-matching USt-IdNr did not stop IBAN resolution, so two vendors sharing a payment-provider IBAN collapsed into one row and the second **overwrote the first's identity**. | `packages/server/src/db/repos/templates.ts:71` | `packages/server/test/component/repos/templates.integrity.test.ts` |
| INVEX-007 | critical | fixed | A stage error returned "processed", skipping the loop's poll sleep. Claims are oldest-first and the row was still under `maxAttempts`, so the next tick re-claimed the **same** document immediately — all three attempts burned in one event-loop burst. One docling restart or one llama-swap 503 during a cold model load permanently `failed`ed every in-flight document. No `uncaughtException` handler either, so a crash left no line naming the document. | `packages/server/src/pipeline/machine.ts:64` | `packages/server/test/component/machine.retry.test.ts` |
| INVEX-009 | high | fixed | `/health` always returned HTTP 200 — a Kubernetes probe could never fail — and reported nothing about the worker, so `{"status":"ok"}` was fully compatible with a server that ingests documents and never processes one. `DEPLOYMENT.md` lists this first in its troubleshooting table and calls it structurally undetectable. | `packages/server/src/http/app.ts:192` | `packages/server/test/component/http/health.test.ts` |
| INVEX-008 | high | fixed | Path `:id` and `?documentId=` were passed unvalidated to a uuid column, so a malformed value raised Postgres 22P02 and Fastify returned **500 with the failed SQL statement and its bound parameters in the body**. `?limit=abc` became `NaN`, survived the clamp, and reached `.limit(NaN)`. | `packages/server/src/http/app.ts:119` | `packages/server/test/component/http/validation.test.ts` |
| INVEX-010 | high | fixed | `vatSynth`'s `v.rate > 0` guard meant a `{rate: 0, tax: "0.00", net: null}` entry — the exact shape `runRuleEngine` emits — could never be completed, so **every §19 Kleinunternehmer and §13b reverse-charge invoice** failed `VAT_INCOMPLETE` and escalated. | `packages/core/src/reconcile/repairs.ts:94` | `packages/core/test/unit/reconcile/zeroRateVat.test.ts` |
| INVEX-013 | medium | open | `d. MMMM yyyy` / `MMMM d, yyyy` are in `DEFAULT_DATE_FORMATS`, but date-fns `parse` is called with no `locale`, so only ENGLISH month names ever match. `"15. Juni 2026"` → null in a German-first pipeline. | `packages/core/src/parsing/dates.ts:12` | `packages/core/test/unit/parsing/dates.test.ts` |
| INVEX-014 | medium | open | `mergeLines` reassigns its cluster anchor to the LAST line rather than the cluster origin, so a ladder of lines each within `yTolerance` of its predecessor collapses into one merged line spanning far more than the tolerance. Dense OCR pages merge unrelated rows. | `packages/core/src/positioned/mergeLines.ts:43` | `packages/core/test/unit/positioned/mergeLines.test.ts` |
| INVEX-017 | high | open | `extractAmountValue` takes the LAST numeric match in the remainder, so `"Nettobetrag 1.148,70 zzgl. 19% MwSt"` yields a net of `19`. The source comment justifies last-match with the opposite ordering — one heuristic cannot serve both. | `packages/core/src/rules/engine.ts:59` | `packages/core/test/unit/rules/engine.test.ts` |
| INVEX-018 | high | open | `"Satz"` is a `table.unitPrice` synonym and column matching is substring-based for synonyms ≥3 chars, so `"Steuersatz"` is claimed as the unit-price column. The winner depends purely on left-to-right column order. 10 cross-column synonym collisions exist in total. | `packages/core/src/rules/lexicon.ts:72` | `packages/core/test/unit/rules/lexicon.test.ts` |
| INVEX-019 | medium | open | The VAT-line rate regex caps the rate at `(\d{1,2})` immediately followed by `%`, so a fractional rate (`"2,5 %"`, CH) never matches and the line is skipped silently. The FIRST occurrence of a rate also wins, so an earlier `"MwSt 19 % 0,00"` line poisons the real one. | `packages/core/src/rules/engine.ts:157` | `packages/core/test/unit/rules/engine.test.ts` |
| INVEX-020 | high | open | An unknown or misspelled weight key silently contributes 0 (`config.weights[name] ?? 0`). Nothing asserts the weights cover the feature set, so a renamed key degrades the classifier to "everything is non_invoice" with no error — for a value the briefing says must be empirically calibrated. | `packages/core/src/classify/classifier.ts:105` | `packages/core/test/unit/classify/classifier.test.ts` |
| INVEX-021 | medium | open | Band thresholds are unvalidated. With `invoiceMin <= nonInvoiceMax` the `invoice` branch is checked first and wins, so the `uncertain` band silently vanishes. | `packages/core/src/classify/classifier.ts:107` | `packages/core/test/unit/classify/classifier.test.ts` |
| INVEX-022 | medium | open | `positionedToMarkdown` appends ALL tables after ALL text regardless of page or position, detaching a table from the prose that introduced it; and `|` in a cell is not escaped, corrupting the emitted Markdown. This is the output a non-invoice is exported as and a downstream LLM reads. | `packages/core/src/classify/classifier.ts:121` | `packages/core/test/unit/classify/classifier.test.ts` |
| INVEX-023 | high | open | CII `fieldMeta` is keyed on the SOURCE index while `lines.push` uses the DESTINATION index, so one skipped line item makes provenance off-by-one for every line after it. Same pattern for `vatBreakdown`. | `packages/core/src/zugferd/cii.ts:198` | `packages/core/test/unit/zugferd/cii.test.ts` |
| INVEX-024 | high | open | Repeated CII elements become arrays, and the `text()`/`node()` helpers return null for arrays. ZUGfERD EXTENDED legitimately emits one `TaxTotalAmount` per currency, so a DECLARED tax total silently becomes null and the solver re-derives it. Same blindness for line-level `RateApplicablePercent` and for multiple `PayeePartyCreditorFinancialAccount` (all IBANs lost). | `packages/core/src/zugferd/cii.ts:187` | `packages/core/test/unit/zugferd/cii.test.ts` |
| INVEX-025 | high | open | A CII line item whose description exists only in `SpecifiedTradeProduct/Description` (not `Name` or `IncludedNote/Content`) is dropped entirely and silently, breaking the line-sum constraint with no indication why. | `packages/core/src/zugferd/cii.ts:204` | `packages/core/test/unit/zugferd/cii.test.ts` |
| INVEX-028 | medium | open | Table rows carrying amounts but no description are dropped silently. The missing money later surfaces as an unexplained `C2_LINE_SUM` violation with no clue where it went. | `packages/core/src/table/lineItems.ts:47` | `packages/core/test/unit/table/lineItems.test.ts` |
| INVEX-029 | medium | open | `descriptionContinuation: "indentedRows"` is accepted by the type but unimplemented — it behaves as `"none"`. Continuation merging is also gated on `columns.position`, so a template without a position column can never merge multi-row descriptions: the very failure mode the file's own doc comment calls most common. | `packages/core/src/table/lineItems.ts:33` | `packages/core/test/unit/table/lineItems.test.ts` |
| INVEX-031 | medium | open | `dd.MM.yy` years 50–89 pivot to 1950–1989 against a `REFERENCE` frozen at year 2000, then fail the 1990–2100 guard and return null — while 00–49 and 90–99 parse fine. Same input shape, opposite outcome, no error. | `packages/core/src/parsing/dates.ts:18` | `packages/core/test/unit/parsing/dates.test.ts` |
| INVEX-032 | low | open | `normalizeLabel` decomposes umlauts via NFD but has no decomposition for `ß`, so it is stripped: `"Straße"` → `strae` vs `"Strasse"` → `strasse`. Both spellings are current in German business writing, and this feeds label matching. `textquality/gate.ts` already does `ß` → `ss`. | `packages/core/src/positioned/model.ts:59` | `packages/core/test/unit/positioned/model.test.ts` |
| INVEX-033 | medium | open | A continuation-shaped row arriving before any item exists falls through to normal item creation (the guard is `items.length > 0`), materialising a phantom line item with no amounts that then participates in the line-sum constraint. | `packages/core/src/table/lineItems.ts:42` | `packages/core/test/unit/table/lineItems.test.ts` |
| INVEX-034 | high | open | A parenthesised qualifier after a label defeats remainder extraction: `normalizeLabel` strips the parens, so the character walk lands before `)` and the remainder begins with `")"`, which `extractAmountValue` rejects. **`packages/fixtures/src/textPdf.ts:27` prints exactly `"Zwischensumme (netto)"`**, so the canonical text fixture never extracts `totals.net` by rule — `R_NET_FROM_LINES` repairs it, and API.md's worked example records that repair as the expected path. A defect became the documented behaviour because fixture and expectation share an author. | `packages/core/src/rules/engine.ts:99` | `packages/core/test/unit/rules/engine.test.ts` |
| INVEX-035 | low | open | CII buyer fields are extracted into `invoice.buyer` but never given a `fieldMeta` entry, unlike every other populated field. Provenance consumers see nothing for buyer data even when it was read cleanly. | `packages/core/src/zugferd/cii.ts:147` | `packages/core/test/unit/zugferd/cii.test.ts` |
| INVEX-015 | high | open | The text gate returns `"ok"` when there is not enough text to judge: `dictHitRate` is only computed above `minTokensForVerdict`, and the verdict is `reasons.length > 0 ? "garbage" : "ok"`. An empty text layer, a numbers-only page, or a short garbage fragment all PASS the very check that exists to reroute unusable text to OCR. | `packages/core/src/textquality/gate.ts:65` | `packages/core/test/unit/textquality/gate.test.ts` |
| INVEX-016 | medium | open | `isAttachmentStart` only fires on the page carrying the AGB/T&C heading, so page 3 of a multi-page terms attachment has no heading, the segment kind flips back, and it becomes a NEW invoice-candidate. The classifier then receives legal boilerplate as an invoice. | `packages/core/src/segment/segmentPages.ts:75` | `packages/core/test/unit/segment/segmentPages.test.ts` |
| INVEX-036 | high | open | The IBAN scan regex is case-sensitive; `normalizeIban` uppercases only AFTER matching, so a lowercase or mixed-case OCR line (`"iban de02120300000000202051"`) yields no IBANs at all — losing tier 3 of the vendor-identity chain exactly on the lane where OCR noise is expected. | `packages/core/src/vendor/extract.ts:40` | `packages/core/test/unit/vendor/extract.test.ts` |
| INVEX-037 | medium | open | Letterhead heuristics are too naive: `nameGuess` takes the first long non-numeric line near the top, so a page header like `"Seite 1 von 2"` becomes the vendor name; `postalCodeGuess` takes the first 5-digit token, which on a German letterhead is frequently the RECIPIENT's postcode. Both feed `nameHash`, the last-resort vendor key. | `packages/core/src/vendor/extract.ts:58` | `packages/core/test/unit/vendor/extract.test.ts` |
| INVEX-038 | high | open | The DE VAT-id scan uses `[\d\s]`, which includes newlines, against the newline-joined document text — so a `DE` ending one line can absorb digits from the next into a **fabricated** VAT id. Demonstrated producing `DE136695976`, which passes the ISO 7064 checksum purely by coincidence. | `packages/core/src/vendor/extract.ts:30` | `packages/core/test/unit/vendor/extract.test.ts` |
| INVEX-040 | medium | open | `zIsoDate` is a bare shape regex with no calendar validation, so `"2026-13-45"` and `"2026-02-30"` are accepted. The VLM path validates straight into `CanonicalInvoice`, so a hallucinated impossible date reaches committed output. | `packages/core/src/schema/invoice.ts:18` | `packages/core/test/unit/schema/vlm.test.ts` |
| INVEX-041 | medium | open | `matchTable`'s fuzzy header match is asymmetric — it tests `header.includes(signature)` only. Short signature cells match almost anything: a bare `"ME"` unit-of-measure signature matches an unrelated `"Nummer"` column at score 1.0. The 0.7 threshold is a magic literal. | `packages/core/src/template/apply.ts:168` | `packages/core/test/unit/template/apply.test.ts` |
| INVEX-042 | high | open | `findField` matches the label against NORMALIZED line text but then locates it in the RAW text by first case-insensitive occurrence, so a line carrying the label twice slices the remainder from the wrong place. Demonstrated returning a superseded invoice number over the current one. | `packages/core/src/template/apply.ts:99` | `packages/core/test/unit/template/apply.test.ts` |
| INVEX-043 | low | open | `applyTemplate` always overwrites `invoice.seller` from `template.vendorIds`, even when every identifier is null, so a template-derived seller block always exists downstream and can mask what extraction actually found. | `packages/core/src/template/apply.ts:233` | `packages/core/test/unit/template/apply.test.ts` |
| INVEX-044 | medium | open | `mergeEnvelopes` merges `seller` and `totals` per sub-field but takes `buyer` wholesale through the generic `pick()`. Three structurally identical nested objects, two different rules — a primary holding only the buyer's name discards a customer number the secondary did find. | `packages/core/src/merge/envelopes.ts:43` | `packages/core/test/unit/merge/envelopes.test.ts` |
| INVEX-045 | medium | open | `fieldMeta` merges key-wise with primary winning, independently of which source won the ARRAY. A primary with `lineItems: []` plus stale per-row metadata stamps its provenance onto rows that came from secondary, so audit tooling credits the wrong extractor for data it never saw. | `packages/core/src/merge/envelopes.ts:15` | `packages/core/test/unit/merge/envelopes.test.ts` |
| INVEX-046 | medium | open | `valuePattern` is an unvalidated regex string compiled with `new RegExp()` at apply time with no try/catch. Every other unmatched descriptor misses cleanly; a malformed pattern in a persisted template throws out of `applyTemplate`, costing the document an attempt. | `packages/core/src/template/apply.ts:72` | `packages/core/test/unit/template/apply.test.ts` |
| INVEX-047 | high | open | **The text gate rejects legible invoices.** Two compounding causes: `runTextGate` reads `doc.lines` only, so it judges the text LAYER while never seeing `doc.tables` — the part of the page carrying most of the words; and `minDictHitRate: 0.55` is far too high for what remains (letterhead, labels, amounts, proper nouns). Three of eight golden scenarios, including a plain German business letter, score 0.47–0.55 and are rerouted to OCR — paying GPU cost on exactly the deterministic path the design exists to widen. Found by the golden corpus; the old suite had one text fixture, which scores 0.65. | `packages/core/src/textquality/gate.ts:50` | `packages/fixtures/test/unit/gateCalibration.test.ts` |
| INVEX-048 | high | open | `openaiCompat` retries ONLY on unparseable JSON. A non-2xx throws immediately, so a **503 while llama-swap swaps models** — which `DEPLOYMENT.md` calls routine, with cold loads measured at 1m29s–2m38s — costs the document an attempt instead of being retried. No `Retry-After`, no backoff. | `packages/server/src/clients/vlm/openaiCompat.ts:59` | `packages/server/test/unit/clients/vlm.openaiCompat.test.ts` |
| INVEX-049 | medium | open | `finish_reason` is never inspected, so a response truncated by the token limit is reported as "non-JSON output" — a capacity problem is indistinguishable from a bad model. A `refusal` is discarded into the same generic message. | `packages/server/src/clients/vlm/openaiCompat.ts:66` | `packages/server/test/unit/clients/vlm.openaiCompat.test.ts` |
| INVEX-050 | medium | open | `stripFences` removes only one leading and one trailing fence. Prose before the JSON, or a `<think>` block from a reasoning VLM, defeats it — and there is no brace-matching fallback. | `packages/server/src/clients/vlm/openaiCompat.ts:98` | `packages/server/test/unit/clients/vlm.openaiCompat.test.ts` |
| INVEX-051 | high | open | The docling client has **no retries at all** despite being the most failure-prone dependency, and **no response validation**: the body is cast to a shape and only `document.json_content` truthiness is checked, so an HTML error page returned with status 200 by a proxy surfaces as a bare `SyntaxError` with no request context. | `packages/server/src/clients/docling.ts:33` | `packages/server/test/unit/clients/docling.test.ts` |
| INVEX-052 | medium | open | `ZUGFERD_NAME_PATTERNS` ends with two UNANCHORED patterns, `/factur/i` and `/zugferd/i`, tested against any attachment name — so `my-zugferd-notes.txt`, `factura.doc` or `refactured.xml` route the whole document to Path A, where it fails and falls through, writing a misleading `xml_fallthrough` escalation row into the log briefing §8 says should drive tuning. | `packages/server/src/pdf/triage.ts:24` | `packages/server/test/unit/pdf/triage.test.ts` |
| INVEX-053 | high | open | The `VLM_ENABLED` override is asymmetric: it sets `enabled` to `value === "true"`, so `VLM_ENABLED=1`, `=yes`, `=TRUE` or `=on` all silently **disable** the VLM. In a deployment whose entire escalation path is switched on by that one variable, a plausible spelling turns it off with no signal. | `packages/server/src/config.ts:84` | `packages/server/test/unit/config.test.ts` |
| INVEX-054 | medium | open | Environment variables are unvalidated while the JSON config is Zod-checked: `PORT=web` yields `NaN` and binds an arbitrary free port, `LOG_LEVEL` is accepted then throws inside pino at import time, and `DATABASE_URL`/`DOCLING_URL`/`VLM_URL` are taken verbatim. | `packages/server/src/config.ts:99` | `packages/server/test/unit/config.test.ts` |
| INVEX-055 | high | open | `POST /api/ingest` accepts any bytes as a PDF — no magic-number check, no media-type validation, and the multipart field name is not checked. With `fileSize: 100MB, files: 50` one request pushes up to 5 GB through `part.toBuffer()` into the Node heap and then into Postgres, the database `DEPLOYMENT.md` identifies as the only copy of every document. The intended entry point is email attachments. | `packages/server/src/http/app.ts:106` | `packages/server/test/component/http/ingest.test.ts` |
| INVEX-057 | high | open | A raw NUL byte in an uploaded filename survives busboy (it is not a path separator) but Postgres TEXT columns reject it, so `insertDocument`'s INSERT throws and Fastify's default handler returns **500 with the failed SQL statement and its bound parameters in the body**. Same internals-disclosure shape as INVEX-008, reached through the FILENAME rather than a path parameter — and the intended entry point is email attachments, where the filename is attacker-controlled. | `packages/server/src/http/app.ts:113` | `packages/server/test/integration/adversarial.ingest.test.ts` |
| INVEX-012 | medium | open | Bare `"Rechnung"` is an `invoiceNumber` label and the value pattern accepts dots, so `"Rechnung 12.06.2026"` yields an invoice number of `12.06.2026`. | `packages/core/src/rules/lexicon.ts:31` | `packages/core/test/unit/rules/engine.test.ts` |
| INVEX-056 | high | open | `POST /api/ingest` is not atomic across the parts of one multipart request: each file is inserted and committed in its OWN transaction as the loop runs, but a later part's failure (oversized file, the `files: 50` limit, a malformed multipart stream) throws out of the loop BEFORE `reply.code(202).send(results)`, so every already-inserted document's id is never returned to the client — the documents are orphaned in the database with no way for the caller to discover them. | `packages/server/src/http/app.ts:100` | `packages/server/test/integration/adversarial.ingest.test.ts` |

<!-- Rows are added in Phase F (fixes, promoted immediately) and Phase 1 (deferred defects,
     pinned only). Keep the table sorted by id. -->
